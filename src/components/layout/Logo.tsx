import { Typography } from '@mui/material';

const Logo = () => {
    return (
        <Typography color="#5f6368" variant="h6" sx={{ display: { xs: 'none', sm: 'block' } }}>
            Drive
        </Typography>
    );
};

export default Logo;
