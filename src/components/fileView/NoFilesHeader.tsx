import { Typography } from '@mui/material';
import React from 'react';

export const NoFilesHeader = () => {
    return <Typography sx={{ color: '#666666', margin: '10px 4px', userSelect: 'none' }}>No files</Typography>;
};
